<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= render partial: "devise/registrations/layouts/edit_form" %>

<h3>Cancel my account</h3>

<p>Unhappy? <%= link_to "Cancel my account", registration_path(resource_name), method: :delete, data: {
    "confirm-swal": "Tem a certeza?",
    "yes-text": "Sim",
    "no-text": "Não"
    }, class: "btn btn-danger" %></p>

<%= link_to "Back", :back, class: "btn btn-secondary"  %>